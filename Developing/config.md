## Configuration Class:

Importing Additional Configuration Classes
--------------
- no need to put all configuration into a single class
- `@Import` can be used to import additional configuration classes.
- `@ComponentScan` can also be used to pick up all Spring Components, including `@Configuration` classes.

